import { TRACE_FORMAT_VERSION } from "@codewalk/trace";

export function App() {
  return (
    <main>
      <h1>codewalk</h1>
      <p>Trace format version {TRACE_FORMAT_VERSION}</p>
    </main>
  );
}
