Copied unchanged from the `orirang` repository (`.agents/skills/react-useeffect`) on 2026-09-20.

Codex loads this directory directly. Claude Code loads the same skill through the relative symlink at `.claude/skills/react-useeffect`.
